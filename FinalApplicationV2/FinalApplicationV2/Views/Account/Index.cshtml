@model IEnumerable<FinalApplicationV2.Models.EditUserViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<table class="table">
    <tr>
            @Html.DisplayNameFor(model => model.Email)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>

        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
        @Html.ActionLink("Roles", "Index", "IdentityRoles")
            </td>
    </tr>
}

</table>
